

const
    RegisterButton = ({ children }) => {

        return (
            <button className='py-[7px] w-[100%] text-[14px] px-[10px] text-center my-m_small rounded-full text-color bg-bg_register_button'>
                {children}
            </button>
        )
    }

export default RegisterButton
